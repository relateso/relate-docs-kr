---
title: "조직 (Organizations)"
description: "Relate API로 조직을 조회·생성·수정·삭제합니다."
sidebarTitle: "Organizations"
"og:title": "조직 (Organizations) | Relate API"
"og:description": "Relate API로 조직을 조회·생성·수정·삭제합니다."
"article:tag": "API"
"twitter:title": "조직 (Organizations) | Relate API"
"twitter:description": "Relate API로 조직을 조회·생성·수정·삭제합니다."
"og:image": "https://raw.githubusercontent.com/relateso/relate-docs-kr/main/docs/images/setup/og-image-docs.png"
"twitter:image": "https://raw.githubusercontent.com/relateso/relate-docs-kr/main/docs/images/setup/og-image-docs.png"
---

Relate API로 조직(Organization)을 조회·생성·수정·삭제합니다.

## 모든 조직 조회

```bash
curl https://api.relate.so/v1/organizations \
  -H "Authorization: Bearer {api_key}"
```

## 조직 단건 조회

```bash
curl https://api.relate.so/v1/organizations/:organization_id \
  -H "Authorization: Bearer {api_key}"
```

## 조직 생성

```bash
curl https://api.relate.so/v1/organizations \
  -H "Authorization: Bearer {api_key}" \
  -H "Content-Type: application/json" \
  -d '{"name":"Relate","domains":["relate.so"]}'
```

- `name`, 또는 `domains`에 유효한 URL이 최소 1개 필요합니다.

## 조직 수정

```bash
curl -X PATCH https://api.relate.so/v1/organizations/:organization_id \
  -H "Authorization: Bearer {api_key}" \
  -H "Content-Type: application/json" \
  -d '{"name":"Relate","domains":["relate.so","relate.kr"],"custom_fields":[{"name":"Category","value":"Startup"}]}'
```

## 조직 도메인 삭제

```bash
curl -X DELETE https://api.relate.so/v1/organizations/:organization_id/domains \
  -H "Authorization: Bearer {api_key}" \
  -H "Content-Type: application/json" \
  -d '{"domains":["relate.kr"]}'
```

## 조직 삭제

```bash
curl -X DELETE https://api.relate.so/v1/organizations/:organization_id \
  -H "Authorization: Bearer {api_key}"
```

