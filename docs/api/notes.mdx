---
title: "노트 (Notes)"
description: "Relate API로 노트를 조회·생성·수정·삭제합니다."
sidebarTitle: "Notes"
"og:title": "노트 (Notes) | Relate API"
"og:description": "Relate API로 노트를 조회·생성·수정·삭제합니다."
"article:tag": "API"
"twitter:title": "노트 (Notes) | Relate API"
"twitter:description": "Relate API로 노트를 조회·생성·수정·삭제합니다."
"og:image": "https://raw.githubusercontent.com/relateso/relate-docs-kr/main/docs/images/setup/og-image-docs.png"
"twitter:image": "https://raw.githubusercontent.com/relateso/relate-docs-kr/main/docs/images/setup/og-image-docs.png"
---

Relate API로 노트(Note)를 조회·생성·수정·삭제합니다.

## 모든 노트 조회

```bash
curl https://api.relate.so/v1/notes \
  -H "Authorization: Bearer {api_key}"
```

## 노트 단건 조회

```bash
curl https://api.relate.so/v1/notes/:note_id \
  -H "Authorization: Bearer {api_key}"
```

## 노트 생성

```bash
curl https://api.relate.so/v1/notes \
  -H "Authorization: Bearer {api_key}" \
  -H "Content-Type: application/json" \
  -d '{"organization_id":"...","deal_id":"...","body":"this is a note"}'
```

- `organization_id`와 `body`는 필수입니다.

## 노트 수정

```bash
curl -X PATCH https://api.relate.so/v1/notes/:note_id \
  -H "Authorization: Bearer {api_key}" \
  -H "Content-Type: application/json" \
  -d '{"body":"this is an edited note"}'
```

- `deal_id`와 `body`만 수정할 수 있습니다.

## 노트 삭제

```bash
curl -X DELETE https://api.relate.so/v1/notes/:note_id \
  -H "Authorization: Bearer {api_key}"
```

